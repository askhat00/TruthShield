# Основной фреймворк
Django>=5.2

# Django REST Framework
djangorestframework
djangorestframework-simplejwt

# PDF обработка
PyMuPDF         # для import fitz
reportlab       # для генерации PDF через canvas

# NLP / ML
transformers
torch           # требуется для transformers

# Настройка окружения и базы данных
python-decouple
dj-database-url

# Дополнительно для работы с временными файлами и потоками (обычно входит в стандартную библиотеку)
# tempfile, io — не требуют установки

# HTTP и парсинг запросов (REST Framework уже покрывает)
# requests — при необходимости для внешних API вызовов
requests

